// https://eddyerburgh.me/toggle-visibility-with-react

import React from 'react';

const SimpleShow = (props) => (

 <div>placing holding some stuff to hide or show{props.stuff}</div>

);

export default SimpleShow;